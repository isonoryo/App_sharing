<div class="container">
  <h2>検索フォーム</h2>
  <%= render 'search_form' %>
</div>

<div class="container">
<h1>新着投稿ポートフォリオ</h1>
  
  <% @portfolios.each do |portfolio| %>
  <div class="card">
   <div class="container">
    <ul>
    <br>
      <li><img src=<%= portfolio.user.profile_image %> class = "icon_image"></li>
      <li>アプリ名<%= portfolio.name %></li>
      <li>作者名<%= portfolio.user.name %></li>
      <li>いいね数<%= render partial: 'portfolios/portfolios', locals: { portfolio: portfolio } %></li>
      <li><%= link_to 'ポートフォリオ詳細へ', portfolio_path(portfolio.id) %></li>
    </ul>
    </div>
    </div>
    <br><br><br>

  <% end %>
  

  <%= link_to 'ポートフォリオ投稿', new_portfolio_path %>
  <%= link_to 'いいねランキングへ', ranking_portfolios_path %>
  <%= link_to 'マイページへ', "/user/#{current_user.id}" %>
</div>